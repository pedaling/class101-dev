// const { initI18n } = require('./src/i18n');
// exports.onPreRenderHTML = ({ pathname }) => {
//   const language = pathname.split('/').filter(Boolean)[0] || 'ko';
//   initI18n(language);
//   console.log({ language });
// };
